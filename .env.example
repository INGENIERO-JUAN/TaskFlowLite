# .env.example — Copiar como .env y completar con valores reales
# El archivo .env real está en .gitignore (nunca commitear)

# URL del backend. Reemplazar con la URL real de Render en producción.
VITE_API_URL=https://taskflow-api-demo.onrender.com/api
